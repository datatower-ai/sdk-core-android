name: publish release
on:
  push:
    branches:
      - main
    tags:
      - core/*

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: ncipollo/release-action@v1