apply from: "conf.gradle"


buildscript {

    // 0 本地
    // 1 maven-snapshot
    // 2 maven-releases
    ext.dependence_type = "2"

    ext.kotlin_version = "1.4.31"
    repositories {
        google()
        jcenter()
        maven { url 'https://www.jitpack.io' }
        maven { url "https://dl.bintray.com/infinum/android" }
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:4.1.3'
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version"
        classpath 'com.kezong:fat-aar:1.3.4'
        classpath 'com.google.gms:google-services:4.3.5'
    }
}

allprojects {
    repositories {
        google()
        jcenter()
        maven { url 'https://www.jitpack.io' }
        maven { url "https://dl.bintray.com/infinum/android" }
        maven(){
            def maven_url =  dependence_type == "1" ? "http://localhost:8081/repository/maven-releases/"
                    : "https://repo-public.en2joy.com/repository/maven-releases/"
            url = maven_url
        }

    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}

